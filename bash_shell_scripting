///substring in bash
x="shell scripting"
echo ${x:2:5}//start position ,how many chars
--ell s.
//strings path
real path -- just appends fname to path
realpath filename
/../dd/sss/filename
basename -- strips path gives filename
basename (complete path)
dirname -- strips the extensions and gives fine name
dirname (comp path) .sh/.bh (extensions)
--pdf there in folder

//getting input ,output from keyboard
read--reads ip from keyboard
$REPLY--if no variable is declared then default variable


command chaining operation

four operators -- ; ,&&,||, && ||.
; -- semicolon // executes cmd2 regardless of success of failure of cmd1
&& -- logical AND cmd2 executes only after success of cmd1
|| -- logical OR cmd2 executes only after failure of cmd1

loops
for(cnt=0;cnt<10;cnt++)
do
  cmd1
  cmd2
done

-->to get 1 to 20 num we have cmd -- seq 1 20


//$@---taking input from the command line

if condition
then
    cmd1
    cmd2
else
    cmd3
    cmd4
fi

//multiple line arguments
<<<start

trats
//
$*--printing all the given elements in 1 line
$@--printing all elements as separate lines





